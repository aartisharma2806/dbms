/* =================================================
   SET 27
   MONGODB MOVIES COLLECTION
   ================================================= */

/* CREATE DATABASE */

use movieDB;


/* CREATE COLLECTION */

db.movies.insertMany([
{
    name:"Movie1",
    type:"action",
    budget:1000000,
    producer:{
        name:"producer1",
        address:"PUNE"
    }
},
{
    name:"Movie2",
    type:"horror",
    budget:500000,
    producer:{
        name:"producer2",
        address:"MUMBAI"
    }
},
{
    name:"Movie3",
    type:"action",
    budget:1500000,
    producer:{
        name:"producer1",
        address:"PUNE"
    }
}
]);


/* =================================================
   i. MOVIES WITH BUDGET > 100000
   ================================================= */

db.movies.find(
{
    budget:{$gt:100000}
},
{
    name:1,
    _id:0
}
);


/* =================================================
   ii. PRODUCERS LIVING IN PUNE
   ================================================= */

db.movies.find(
{
    "producer.address":"PUNE"
},
{
    "producer.name":1,
    _id:0
}
);


/* =================================================
   iii. MOVIES OF TYPE ACTION OR HORROR
   ================================================= */

db.movies.find(
{
    type:{$in:["action","horror"]}
}
);


/* =================================================
   iv. DOCUMENTS PRODUCED BY producer1
   WITH ADDRESS
   ================================================= */

db.movies.find(
{
    "producer.name":"producer1"
},
{
    name:1,
    "producer.address":1,
    _id:0
}
);
