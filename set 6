Create Employees Table
CREATE TABLE Employees(
    employee_id NUMBER(6) PRIMARY KEY,
    first_name VARCHAR2(20),
    last_name VARCHAR2(25) NOT NULL,
    email VARCHAR2(25) NOT NULL,
    phone_number VARCHAR2(20),
    hire_date DATE NOT NULL,
    job_id VARCHAR2(10) NOT NULL,
    salary NUMBER(8,2),
    commission_pct NUMBER(2,2),
    manager_id NUMBER(6),
    department_id NUMBER(4)
);
Create Departments Table
CREATE TABLE Departments(
    department_id NUMBER(4) PRIMARY KEY,
    department_name VARCHAR2(30) NOT NULL,
    manager_id NUMBER(6),
    location_id NUMBER(4)
);
Insert Sample Records
Departments
INSERT INTO Departments VALUES(10,'IT',101,1001);
INSERT INTO Departments VALUES(20,'HR',102,1002);
INSERT INTO Departments VALUES(30,'Sales',103,1003);
Employees
INSERT INTO Employees VALUES
(101,'Vaishali','Patil','vpatil@gmail.com','9876543210',
TO_DATE('10-01-2020','DD-MM-YYYY'),
'IT_PROG',50000,0.10,201,10);

INSERT INTO Employees VALUES
(102,'Amit','Sharma','amit@gmail.com','9876543211',
TO_DATE('15-02-2021','DD-MM-YYYY'),
'HR_MGR',40000,0.05,202,20);

INSERT INTO Employees VALUES
(103,'Neha','Joshi','neha@gmail.com','9876543212',
TO_DATE('20-03-2022','DD-MM-YYYY'),
'SALESMAN',45000,0.08,203,30);
1) Create EmpTest from Employees Table
CREATE TABLE EmpTest AS
SELECT * FROM Employees;


2) Add New Row into EmpTest
INSERT INTO EmpTest
(employee_id, first_name, salary)
VALUES
(104, USER, 30000);

3) Increase Salary of Vaishali by 15% and Confirm Changes
UPDATE EmpTest
SET salary = salary + (salary*0.15)
WHERE first_name='Vaishali';
COMMIT;

4) Increase Salary by 10% for Employees Working in PUNE

UPDATE EmpTest
SET salary = salary + (salary*0.10)
WHERE department_id = 10;

5) Delete All Commission Data
UPDATE EmpTest
SET commission_pct = NULL;

6) Delete Employees in Dept No 10 Without Commit
DELETE FROM EmpTest
WHERE department_id = 10;

7) Rollback Changes
ROLLBACK;
