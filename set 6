CREATE TABLE Employees (
    EMPLOYEE_ID NUMBER(6) PRIMARY KEY,
    FIRST_NAME VARCHAR2(20),
    LAST_NAME VARCHAR2(25) NOT NULL,
    EMAIL VARCHAR2(25) NOT NULL,
    PHONE_NUMBER VARCHAR2(20),
    HIRE_DATE DATE NOT NULL,
    JOB_ID VARCHAR2(10) NOT NULL,
    SALARY NUMBER(8,2),
    COMMISSION_PCT NUMBER(2,2),
    MANAGER_ID NUMBER(6),
    DEPARTMENT_ID NUMBER(4)
);

CREATE TABLE Departments (
    DEPARTMENT_ID NUMBER(4) PRIMARY KEY,
    DEPARTMENT_NAME VARCHAR2(30) NOT NULL,
    MANAGER_ID NUMBER(6),
    LOCATION_ID NUMBER(4)
);

INSERT INTO Departments VALUES (10, 'HR', 101, 1001);
INSERT INTO Departments VALUES (20, 'Finance', 102, 1002);
INSERT INTO Departments VALUES (30, 'IT', 103, 1003);

INSERT INTO Employees VALUES
(101, 'Vaishali', 'Sharma', 'vaishali@gmail.com',
'9876543210', TO_DATE('2022-01-10','YYYY-MM-DD'),
'HR01', 50000, 0.10, 201, 10);

INSERT INTO Employees VALUES
(102, 'Rahul', 'Patil', 'rahul@gmail.com',
'9123456780', TO_DATE('2021-03-15','YYYY-MM-DD'),
'FIN01', 60000, 0.15, 202, 20);

INSERT INTO Employees VALUES
(103, 'Amit', 'Joshi', 'amit@gmail.com',
'9988776655', TO_DATE('2020-07-20','YYYY-MM-DD'),
'IT01', 70000, 0.20, 203, 30);

COMMIT;

CREATE TABLE EmpTest AS
SELECT * FROM Employees;


INSERT INTO EmpTest (EMPLOYEE_ID, FIRST_NAME, SALARY,
LAST_NAME, EMAIL, HIRE_DATE, JOB_ID)
VALUES
(104, USER, 45000,
'Default', 'default@gmail.com',
SYSDATE, 'TEMP01');

SELECT * FROM EmpTest;


UPDATE EmpTest
SET SALARY = SALARY + (SALARY * 0.15)
WHERE FIRST_NAME = 'Vaishali';

SELECT FIRST_NAME, SALARY
FROM EmpTest
WHERE FIRST_NAME = 'Vaishali';


UPDATE EmpTest
SET SALARY = SALARY + (SALARY * 0.10)
WHERE EMPLOYEE_ID IN (
    SELECT E.EMPLOYEE_ID
    FROM Employees E
    JOIN Departments D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
    WHERE D.LOCATION_ID = 1003
);

SELECT EMPLOYEE_ID, FIRST_NAME, SALARY
FROM EmpTest;


UPDATE EmpTest
SET COMMISSION_PCT = NULL;

SELECT EMPLOYEE_ID, COMMISSION_PCT
FROM EmpTest;


DELETE FROM EmpTest
WHERE DEPARTMENT_ID = 10;

SELECT * FROM EmpTest;


ROLLBACK;

SELECT * FROM EmpTest;
