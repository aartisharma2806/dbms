

CREATE TABLE STUDENT(
    S_ID NUMBER(5) PRIMARY KEY,
    NAME VARCHAR2(30),
    DEPT_NAME VARCHAR2(30),
    TOT_CRED NUMBER(5)
);

CREATE TABLE INSTRUCTOR(
    T_ID NUMBER(5) PRIMARY KEY,
    NAME VARCHAR2(30),
    DEPT_NAME VARCHAR2(30),
    SALARY NUMBER(10,2)
);



CREATE TABLE COURSE(
    COURSE_ID VARCHAR2(10) PRIMARY KEY,
    TITLE VARCHAR2(50),
    DEPT_NAME VARCHAR2(30),
    CREDITS NUMBER(2)
);


CREATE TABLE TAKES(
    S_ID NUMBER(5),
    COURSE_ID VARCHAR2(10),
    FOREIGN KEY (S_ID) REFERENCES STUDENT(S_ID),
    FOREIGN KEY (COURSE_ID) REFERENCES COURSE(COURSE_ID)
);



INSERT INTO STUDENT VALUES(1,'AMOL','COMPUTER',20);
INSERT INTO STUDENT VALUES(2,'AMIT','IT',18);
INSERT INTO STUDENT VALUES(3,'RAM','COMPUTER',22);
INSERT INTO STUDENT VALUES(4,'SAM','COMPUTER',25);

INSERT INTO INSTRUCTOR VALUES(101,'RAJ','COMPUTER',50000);
INSERT INTO INSTRUCTOR VALUES(102,'AMOL','IT',40000);
INSERT INTO INSTRUCTOR VALUES(103,'KIRAN','COMPUTER',45000);
INSERT INTO INSTRUCTOR VALUES(104,'AMIT','MECH',30000);

INSERT INTO COURSE VALUES('C1','DBMS','COMPUTER',4);
INSERT INTO COURSE VALUES('C2','JAVA','IT',3);

INSERT INTO TAKES VALUES(1,'C1');
INSERT INTO TAKES VALUES(3,'C1');
INSERT INTO TAKES VALUES(4,'C1');

COMMIT;



SELECT DEPT_NAME,
       AVG(SALARY) AS AVG_SALARY
FROM INSTRUCTOR
GROUP BY DEPT_NAME
HAVING AVG(SALARY) > 42000;



UPDATE INSTRUCTOR
SET SALARY = SALARY + (SALARY * 10/100)
WHERE DEPT_NAME = 'COMPUTER';



SELECT NAME
FROM INSTRUCTOR
WHERE NAME NOT IN ('AMOL','AMIT');



SELECT NAME
FROM STUDENT
WHERE LOWER(NAME) LIKE '%am%';



SELECT S.NAME
FROM STUDENT S
JOIN TAKES T
ON S.S_ID = T.S_ID
JOIN COURSE C
ON T.COURSE_ID = C.COURSE_ID
WHERE S.DEPT_NAME = 'COMPUTER'
AND C.TITLE = 'DBMS';
