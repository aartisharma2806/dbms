/* =================================================
   SET 33
   MONGODB EMPLOYEE DATABASE
   ================================================= */

/* 1. CREATE DATABASE */

use Employee;


/* 2. CREATE COLLECTION */

db.createCollection("emp1");


/* =================================================
   3. INSERT DOCUMENTS
   ================================================= */

db.emp1.insertMany([
{
    eno:101,
    ename:"Amit",
    address:"Pune",
    sal:12000
},
{
    eno:102,
    ename:"Neha",
    address:"Mumbai",
    sal:18000
},
{
    eno:103,
    ename:"Rohit",
    address:"Nashik",
    sal:22000
},
{
    eno:104,
    ename:"Seema",
    address:"Delhi",
    sal:8000
}
]);


/* =================================================
   4. DISPLAY ALL DOCUMENTS
   ================================================= */

db.emp1.find();


/* =================================================
   5. EMPLOYEES WITH SALARY > 5000
   ================================================= */

db.emp1.find({
    sal:{$gt:5000}
});


/* =================================================
   6. EMPLOYEES WITH SALARY < 15000
   ================================================= */

db.emp1.find({
    sal:{$lt:15000}
});


/* =================================================
   7. EMPLOYEES WITH SALARY
   BETWEEN 10000 AND 20000
   ================================================= */

db.emp1.find({
    sal:{$gt:10000,$lt:20000}
});


/* =================================================
   8. UPDATE ALL EMPLOYEES
   WITH 10% INCREMENT
   ================================================= */

db.emp1.updateMany(
{},
{
    $mul:{sal:1.10}
}
);


/* =================================================
   9. DELETE EMPLOYEE
   WITH SALARY < 5000
   ================================================= */

db.emp1.deleteMany({
    sal:{$lt:5000}
});


/* =================================================
   10. RENAME COLLECTION
   ================================================= */

db.emp1.renameCollection("employee1");


/* =================================================
   11. EMPLOYEE NAME STARTS WITH N
   ================================================= */

db.employee1.find({
    ename:/^n/i
});


/* =================================================
   12. SORT NAME ASCENDING
   ================================================= */

db.employee1.find().sort({
    ename:1
});


/* =================================================
   13. CREATE DATABASE Employee1
   ================================================= */

use Employee1;


/* =================================================
   14. DROP Employee1 DATABASE
   ================================================= */

db.dropDatabase();


/* =================================================
   15. CREATE COLLECTION emp1
   USING INSERT METHOD
   ================================================= */

db.emp1.insertOne({
    eno:201,
    ename:"Karan",
    address:"Pune",
    sal:15000
});


/* =================================================
   16. DROP COLLECTION emp1
   ================================================= */

db.emp1.drop();
