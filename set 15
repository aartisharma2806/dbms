CREATE TABLE EMP (
    EMPNO NUMBER(5) PRIMARY KEY,
    ENAME VARCHAR2(30),
    SAL NUMBER(10,2)
);

DECLARE
    V_EMPNO EMP.EMPNO%TYPE := &EMPNO;
    V_ENAME EMP.ENAME%TYPE := '&ENAME';
    V_SAL   EMP.SAL%TYPE := &SAL;
    
    V_COUNT NUMBER;
BEGIN
    /* CHECK WHETHER EMPNO EXISTS */
    SELECT COUNT(*)
    INTO V_COUNT
    FROM EMP
    WHERE EMPNO = V_EMPNO;

    /* IF EMPNO DOES NOT EXIST */
    IF V_COUNT = 0 THEN
        INSERT INTO EMP (EMPNO, ENAME, SAL)
        VALUES (V_EMPNO, V_ENAME, V_SAL);

        DBMS_OUTPUT.PUT_LINE('RECORD INSERTED');

    /* IF EMPNO EXISTS */
    ELSE
        UPDATE EMP
        SET ENAME = V_ENAME,
            SAL = V_SAL
        WHERE EMPNO = V_EMPNO;

        DBMS_OUTPUT.PUT_LINE('RECORD UPDATED');
    END IF;

    COMMIT;
END;
/
